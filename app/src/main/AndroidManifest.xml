<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.haydenfarnsworth.studentsupport">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".activity.EditCourseActivity"></activity>
        <activity android:name=".activity.EditAssessmentActivity" />
        <activity android:name=".activity.NewAssessmentActivity" />
        <activity android:name=".activity.NewCourseActivity" />
        <activity
            android:name=".activity.MainActivity"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".util.NotificationPublisher" />
    </application>

</manifest>